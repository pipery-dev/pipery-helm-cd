# Test Project - Helm CD

This fixture is a representative Helm chart with deployment and service
templates. CI uses the `mock-bin/helm` shim to exercise upgrade and status
paths without a Kubernetes cluster.

