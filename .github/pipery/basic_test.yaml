name: basic-test
description: "Test pipery-helm-cd action, skip advanced steps"
source_path: test-project
inputs:
  release_name: "test-release"
  chart: "."
  namespace: "pipery-test"
  skip_deploy: "true"
  skip_status_check: "true"
expect:
  log_path: pipery.jsonl
  success_values: [success, passed, ok, succeeded]
  required_fields:
    - name: event
      value: deploy
